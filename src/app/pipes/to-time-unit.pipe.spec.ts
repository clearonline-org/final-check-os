import { ToTimeUnitPipe } from './to-time-unit.pipe';

describe('ToTimeUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new ToTimeUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
